// Generated by CoffeeScript 1.6.3
(function() {
  var _ref,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    _this = this;

  module("ashtag.panes");

  ashtag.panes.BuyTagsPane = (function(_super) {
    __extends(BuyTagsPane, _super);

    function BuyTagsPane() {
      this.updatePrice = __bind(this.updatePrice, this);
      _ref = BuyTagsPane.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    BuyTagsPane.prototype.initialise = function() {
      this.includeShippingInPrice = true;
      this.$tagPackInput = this.$('.select-tags select');
      this.$quantityInput = this.$('.quantity select');
      return this.$price = this.$('.price-placehodler');
    };

    BuyTagsPane.prototype.setupEvents = function() {
      this.$tagPackInput.on('change', this.updatePrice);
      return this.$quantityInput.on('change', this.updatePrice);
    };

    BuyTagsPane.prototype.start = function() {
      return this.updatePrice();
    };

    BuyTagsPane.prototype.updatePrice = function() {
      var formattedPrice;
      formattedPrice = Number(this.getPrice()).toFixed(2);
      return this.$price.text(formattedPrice);
    };

    BuyTagsPane.prototype.getPrice = function() {
      var price;
      price = this.getPackPrice() * this.getQuantity();
      if (this.includeShippingInPrice) {
        price += this.getShipping();
      }
      return price;
    };

    BuyTagsPane.prototype.getShipping = function() {
      return 3;
    };

    BuyTagsPane.prototype.getPackPrice = function() {
      var packPrice;
      packPrice = this.$tagPackInput.find('option:selected').data('price');
      return parseInt(packPrice, 10);
    };

    BuyTagsPane.prototype.getQuantity = function() {
      var quantity;
      quantity = this.$quantityInput.find('option:selected').val();
      return parseInt(quantity, 10);
    };

    return BuyTagsPane;

  })(ashtag.lib.panes.BasePane);

  $(window).on('pagechange', function(event, obj) {
    if (obj.toPage.attr('id') === 'buy-tags-page') {
      return new ashtag.panes.BuyTagsPane(obj.toPage);
    }
  });

}).call(this);
